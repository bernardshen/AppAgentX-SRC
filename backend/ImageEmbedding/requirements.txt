# Pinned 2026-06-18 — shares the backend venv with OmniParser; runs on spark GB10 GPU.
# torch overridden from the old 2.2.0 (CPU) pin to the cu130 CUDA build (see docs §B.1).
--extra-index-url https://download.pytorch.org/whl/cu130
fastapi==0.110.0
uvicorn==0.27.0
torch==2.9.1+cu130            # was 2.2.0 (CPU) — this old pin was what kept dragging torch back to CPU
torchvision==0.24.1          # was 0.17.0
timm==0.9.12
pillow==10.2.0
pydantic==2.6.0
python-multipart==0.0.9
numpy==1.26.0
