# Pinned 2026-06-18 — verified working on spark (aarch64, NVIDIA GB10) with ZERO OmniParser code changes.
# torch/torchvision come from the PyTorch cu130 index (CUDA build for GB10/Blackwell sm_121).
# Canonical install is the 2-step recipe in docs §B.1 (uv --torch-backend=cu130); this file is the pinned manifest.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.9.1+cu130
torchvision==0.24.1
easyocr==1.7.2
supervision==0.18.0
openai==1.3.5
transformers==4.45.2
ultralytics==8.3.70            # was 8.1.24 — icon_detect_v1_5 weights are YOLO11, need >=8.3 (C3k2 module)
azure-identity==1.25.3
numpy==1.26.0
opencv-python==4.6.0.66
opencv-python-headless==4.11.0.86
gradio==5.1.0
dill==0.4.1
accelerate==1.14.0
timm==0.9.12
einops==0.8.0
paddlepaddle==2.6.2            # was unpinned (3.x) — 3.x dropped PaddleOCR's use_gpu/show_log kwargs
paddleocr==2.7.0.3            # was unpinned (3.x)
setuptools==82.0.1            # paddle cpp_extension imports setuptools; uv venv does NOT include it
wheel==0.47.0
